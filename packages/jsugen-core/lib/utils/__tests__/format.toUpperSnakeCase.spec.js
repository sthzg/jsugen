// import { toUpperSnakeCase } from '../format';

describe('toUpperSnakeCase', () => {
  test('should sanitize', () => {
    // const test = [
    //   toUpperSnakeCase('anyValue'),
    //   toUpperSnakeCase('any-value'),
    //   toUpperSnakeCase('any_value'),
    // ];
    //
    // console.log(JSON.stringify(test, null, 2));
    //
    // expect(Array.from(new Set(test))).toEqual(['ANY_VALUE']);
  });
});
